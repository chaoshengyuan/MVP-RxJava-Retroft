<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".module.MainActivity">
    <RelativeLayout
        android:id="@+id/layout_header"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:orientation="vertical"
        android:background="@drawable/header_bg_shape"
        >
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:textColor="@color/white"
            android:textSize="24sp"
            android:text="登录"/>
    </RelativeLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_below="@+id/layout_header"
        android:background="@drawable/corner_view"
        android:layout_marginTop="-40dp"
        android:layout_marginRight="20dp"
        android:layout_marginLeft="20dp"
        android:layout_marginBottom="120dp"
        android:paddingTop="20dp"
        >
        <com.wenbing.mvpdemo.widget.InputView
            android:id="@+id/et_username"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_marginLeft="40dp"
            android:layout_marginRight="40dp"
            app:leftIcon="@drawable/account_name"
            app:delIcon="@drawable/ic_delete"
            app:hint="请输入用户名"
            />
        <com.wenbing.mvpdemo.widget.InputView
            android:id="@+id/et_password"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_marginLeft="40dp"
            android:layout_marginRight="40dp"
            app:leftIcon="@drawable/account_password"
            app:delIcon="@drawable/ic_delete"
            app:eyeIcon="@drawable/ic_eye"
            app:isPassword="true"
            app:hint="请输入密码"
            />
        <Button
            android:id="@+id/btn_login"
            android:layout_width="match_parent"
            android:layout_height="38dp"
            android:layout_marginTop="40dp"
            android:layout_marginLeft="60dp"
            android:layout_marginRight="60dp"
            android:background="@drawable/button_click_selector"
            android:textColor="@color/blue"
            android:text="登录"
            >

        </Button>
    </LinearLayout>

</RelativeLayout>